# Netlify build config — static site, no build step needed
[build]
  publish = "."        # serve files from the root of this folder
  command = ""         # no build command (plain HTML/CSS/JS)

# Clean URLs
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
